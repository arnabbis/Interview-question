const {Index}=require("typeorm");

const products = [
    { name: "Product A", quantity: 10 },
    { name: "Product B", quantity: 5 },
    { name: "Product C", quantity: 15 },
    { name: "Product D", quantity: 3 }
  ];
  
//   const minQuantity = products.reduce((min, product) => {
//     return product.quantity < min ? product.quantity : min;
//   },Infinity);

const findMaxQuantity = 

  console.log(findMaxQuantity)